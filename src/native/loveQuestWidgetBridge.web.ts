import type { LoveQuestWidgetBridgePlugin } from './loveQuestWidgetBridge';

const LoveQuestWidgetBridgeWeb: LoveQuestWidgetBridgePlugin = {
  async saveWidgetData() {
    /* Web: no-op */
  },
  async reloadWidget() {
    /* Web: no-op */
  },
};

export default LoveQuestWidgetBridgeWeb;
